# Roadmap

- Add optional image thumbnail contact sheets for visual review.
- Add optional duplicate grouping by hash without deletion.
- Add richer package-directory handling for `.photoslibrary`, `.app`, `.pages`,
  `.numbers`, `.key`, and code workspaces.
- Add machine-readable policy files for user-defined taxonomy rules.
- Add a small text UI for reviewing manifests row by row.
